version: "3.9"

services:
  lagouttedor:
    build: .
    container_name: la-goutte-dor
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - EDITOR_TOKEN=change-me-in-production
      - DATA_DIR=/app/data
    volumes:
      - ./data:/app/data
